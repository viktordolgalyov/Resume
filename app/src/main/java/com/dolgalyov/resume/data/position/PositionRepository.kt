package com.dolgalyov.resume.data.position

import com.dolgalyov.resume.common.arch.data.AbstractRepository
import com.dolgalyov.resume.data.position.model.PositionDto

typealias PositionRepository = AbstractRepository<String, PositionDto>